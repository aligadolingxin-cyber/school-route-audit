# 資料前處理

## 人行道

```bash
uv run pipeline/build_sidewalk.py --src <SIDEWALK_202606.zip>
uv run pipeline/build_sidewalk.py --src <...> --county 台北市
```

需要 [uv](https://docs.astral.sh/uv/)，會自動取得 Python 與 shapely。原始 zip 不納入版本庫，自 `data.gov.tw/dataset/58791` 下載後置於 `data/raw/`。

輸出至 `app/data/sidewalk/<縣市>.geojson` 與 `index.json`。這些檔案**納入版本庫**——GitHub Pages 直接自版本庫提供服務，它們即是部署產物（design D11）。

### 處理步驟與理由

**1. 去除完全重複的紀錄。** 台北市有 12 組幾何與屬性皆相同的紀錄。這是來源的資料品質問題。18,304 → 18,292。

**2. 產生識別碼。** 來源沒有 ID 欄位。語意鍵（區＋路名＋起訖＋側別）在台北市只有 7,417 個相異值對 18,304 筆——「忠孝東路七段／向陽路」一組就重複 26 次，加入路段長度後仍有 457 筆重複。故識別碼為 SHA-256 前 12 碼，輸入含語意鍵與幾何代表點（捨入至小數 6 位）。

碰撞時腳本以非零碼結束且不輸出。識別碼會被 `school-route-audit-mvp` 的評估紀錄引用，碰撞等同把評估結果掛到錯的路段上。

**3. 座標捨入至小數 6 位。** 來源存到 12 位，那是 0.1 奈米等級；而來源本身的平面精度是 2.5 公尺。6 位約 0.11 公尺，仍遠高於需要。

**4. 幾何簡化，容差 0.3 公尺。** 人行道寬度中位數僅 1.35 公尺，0.3 公尺已是其兩成，不宜再大。實測 0.5 公尺僅再省 8% 傳輸量，不值得多出的失真。簡化後若變成空幾何或非面幾何，退回原幾何。

**簡化不影響淨寬分級**——淨寬是屬性（`SWW_WTH`），原值保留，與幾何無關。

**5. 移除 `COUNTY_NA`。** 檔案已分縣市，此欄冗餘。

**6. 輸出壓平的 JSON。** 來源是 pretty-print，**61.8% 的位元組是空白字元**。

### 台北市實測

| 設定 | 原始 | gzip（實際傳輸） |
|---|---:|---:|
| 來源（pretty-print，12 位小數） | 49.96 MB | 5.53 MB |
| 壓平 | 18.32 | 5.05 |
| 壓平 + 6 位小數 | 13.57 | 2.29 |
| **壓平 + 6 位 + 簡化 0.3 m**（採用） | **8.61** | **1.48** |
| 壓平 + 6 位 + 簡化 0.5 m | 7.96 | 1.19 |
| 壓平 + 5 位小數 | 12.78 | 1.49 |

頂點數減少 58%。GitHub Pages 自動以 gzip 提供服務，故決定載入速度的是右欄。

前端實測：載入 712 ms、繪製 228 ms。

### 決定性

相同輸入重跑產出的檔案**位元組完全相同**（SHA-256 驗證）。識別碼不含亂數、時間或列舉順序。

## 學校

```bash
uv run pipeline/build_schools.py
uv run pipeline/build_schools.py --county 臺北市
```

來源：國土測繪中心「各級學校範圍圖_121分帶」（shapefile，EPSG:3826）＋ 教育部 115 學年五份名錄 ＋ OSM 縣市界（快取於 `data/raw/tw_counties.json`）。輸出至 `app/data/schools/`。

### 學級判定

**校名後綴為主，教育部名錄為佐證。** 這與原設計相反，改動的理由見 design D3：名錄接合率僅 82%，且三成以上的接合結果同名多學級（臺北市的「大安」同時有大安國小與大安國中），名錄無法單獨決定學級。

實測 4,336 個校地：一致 80.6%、不一致 1.2%（採名錄）、僅名稱可判 10.8%、僅名錄可判 0.2%、大專分部校區 4.7%、無法判定 2.6%。不一致與無法判定者列於 `docs/school-join-report.json`。

接合的三個坑：

1. **校名格式在學級之間不一致。** 國小是「南投縣中寮鄉中寮國民小學」，國中卻是「南投縣立三光國民中學」——那個「立」屬於「縣立」。砍掉縣市後殘留的「立」若不處理，核心名變成「立三光」，國中的接合率會幾乎歸零。
2. **名錄用簡稱，地圖用全名。** 基隆女中 vs 基隆女子高級中學。
3. **大專的分部與校區不在名錄中**，另以名稱樣式（校區／分部／分校／推廣教育／教育中心／館）歸入大專。

### 縣市判定

**由座標決定，不由校名。** 「國立仁愛高級農業職業學校」的「仁愛」是南投縣仁愛鄉不是縣名；以校名判定時有 3.5% 無法歸屬且集中於國立學校。

**飛地必須取面積最小者。** 臺北市完整位於新北市之內、嘉義市位於嘉義縣之內；自邊界線段組面時內孔會被填實，一個點會同時落在兩個縣市內。初版取第一個命中者，導致臺北市整個消失、其 359 所學校全被歸入新北市。

改正後 4,336 校全數判定，與校名判定零衝突。

### 已知缺漏

來源為**121 分帶**，不含澎湖縣與金門縣（在 119 分帶的另一個檔案）。連江縣僅 2 筆，數量可疑，一併待查。

## 道路中心線（查無人行道紀錄）

```bash
uv run pipeline/build_roads.py --county 臺北市
uv run pipeline/build_roads.py --county 臺北市 --sensitivity
```

來源：OpenStreetMap，經 Overpass 依縣市外接矩形取得後裁入縣市界。查詢慢（台北市約 4 分鐘、34.8 MB），故快取於 `data/raw/roads_<縣市>.json`。

**只輸出查無人行道紀錄的道路。** 有人行道者已由人行道多邊形圖層呈現，再輸出一次只增加檔案大小。統計數字（總段數、總長度）寫入 `index.json`，供介面計算比例。

### 排除規則

排除國道與快速道路——學童不會走那裡，而它們會在地圖上造成大量與通學無關的灰線。另排除 `service` 底下的 `parking_aisle`、`driveway`、`drive-through`。

台北市：OSM 48,218 條 → 過濾後 44,048 → 裁入縣市界後 25,780 段。

### 比對方式

**空間鄰近，不是路名字串。** 於 EPSG:3826 將道路中心線緩衝指定公尺數，以 STRtree 查詢是否與任何人行道多邊形相交。

勘查階段曾以路名比對得到大安區 55.9%，但路名格式不一（巷弄層級、全半形、有無「段」），字串比對會把格式差異誤判為沒有人行道。

### 判定距離與其敏感度

**採 20 公尺**，此數字必須與結果一併呈現——比例對它敏感：

| 判定距離 | 查無人行道紀錄（台北市 25,780 段） |
|---:|---:|
| 10 m | 39.4% |
| 15 m | 35.1% |
| **20 m** | **32.7%** |
| 25 m | 30.5% |
| 30 m | 28.8% |

單一距離在物理上並不正確：寬路的人行道離中心線遠，窄巷則近。距離取小會使寬路被誤判為沒有人行道；取大會使巷弄被鄰街的人行道誤配而掩蓋問題。後者是比較危險的方向，因為本工具要講的正是巷弄。依道路等級調整距離會更正確，列為待辦。

### 台北市結果

以 20 m 判定，8,422 / 25,780 段（32.7%）、1,009 / 3,343 km（30.2%）查無人行道紀錄。其中 `service` 5,394 段、`residential` 1,903 段——即巷弄與住宅區道路。

輸出 2.23 MB / gzip 0.39 MB。

## 更新資料時

來源為「不定期更新」。改版後重跑腳本，並**比對前後版本的識別碼集合**：代表點位於識別碼的輸入中，故幾何若調整超過 0.11 公尺，該路段的識別碼會改變。消失的識別碼代表既有評估紀錄失去對應，須另行處理（design D9）。
