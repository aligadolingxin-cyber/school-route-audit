# Tasks

## 1. 判準

- [x] 1.1 下載 MAPS-Mini 問卷、資料字典與圖解手冊 — 存於 `docs/instruments/`，來源、日期、SHA-256 與授權但書記於 `SOURCE.md`
- [x] 1.2 定義判準綱要 — **改用 JSON 而非 YAML**（瀏覽器無內建 YAML 解析器，本專案不使用建置工具鏈，為讀設定檔引入函式庫不划算）
- [x] 1.3 轉錄 MAPS-Mini 15 項 — 滿分 21 與原工具一致。路口題限定於「該路段終點的路口」（單位型別的調整，記於判準檔 `adaptations`）；第 6、7 題標記 `low_reliability`
- [x] 1.4 擬定台北在地 8 項（滿分 13）— 騎樓有無／連續性／被佔用、機車停放、路邊停車侵佔、無人行道路段的步行空間區分、車速抑制、人車無區隔。**全部標記 `draft: true`**
- [ ] 1.4b 對照內政部建研所「騎樓及人行道路無障礙改善研究」檢視在地項目後移除 `draft` 標記
- [ ] 1.5 實作判準檔載入與驗證，對缺漏欄位、重複項目識別碼、未知來源標記各給出指明位置的錯誤，以刻意損壞的樣本檔驗證
- [ ] 1.6 實作計分：「無法判定」自分子與分母扣除（design D5），以含無法判定項目的樣本驗證分數非以 0 計入

## 2. 待評清單

- [x] 2.1 挑出示範單位 — 12 段，大安國中／建安國小中點半徑 260 m 內；紅 4、黃 3、綠 2、查無紀錄 3
- [x] 2.2 實作清單載入與識別碼解析 — 對應不到者於狀態列明確列出識別碼，不靜默略過
- [x] 2.3 確認識別碼存在 — 12 個全數對應成功，無重複
- [x] 2.4 記錄挑選依據 — `docs/demo-units.md`

## 3. 工作台介面

- [x] 3.1 建立 `app/audit.html` — 與地圖共用部署與樣式；首頁有連結。另修部署流程只處理 index.html 的問題
- [x] 3.2 街景與判準表並排 — 線上實測兩者同時可見；窄視窗改為上下排列
- [x] 3.3 引用 `app/js/streetview.js`（design D4）— 未另寫一份街景邏輯，實測影像日期 2025-02
- [ ] 3.4 實作沿路段前後移動視點，確認移動期間不離開目前單位、已作答內容不變
- [x] 3.5 呈現影像拍攝日期
- [x] 3.6 無影像的單位可標記為待現地補拍
- [x] 3.7 單位間前後導覽與跳至指定單位 — 實測切回時作答完整還原
- [x] 3.8 呈現整體完成進度與各單位狀態 — 已完成／進行中／待補拍／未開始

## 4. 作答與保存

- [x] 4.1 依判準檔產生表單並即時寫入 — 實測重載後作答仍在
- [x] 4.2 重新開啟時回到上次單位並還原作答 — 實測四個作答含「無法判定」的原因文字皆還原
- [x] 4.3 三種狀態 — 實測部分作答後為「進行中」，各區段顯示已答項數
- [x] 4.4 「無法判定」作為作答值並記錄原因 — 實測計分為 4/5（80%），該項自分子與分母一併扣除
- [x] 4.5 每筆紀錄寫入判準版本

## 5. 耗時記錄

- [x] 5.1 計時自單位開啟起算並於切換時停止 — 實測停留 6 秒後切換，存下 33 秒（含先前停留），回來顯示 34 秒含即時段
- [x] 5.1b 改為每 5 秒定期落盤 — 只在切換時落盤會使頁面關閉時掉掉進行中那一段，實測曾掉 31 秒
- [ ] 5.2 切換瀏覽器分頁不暫停計時，以切走後返回驗證
- [x] 5.3 每單位耗時統計（中位數、平均、總計）— 已完成單位達一個以上時顯示於進度列

## 6. 匯出與匯入

- [x] 6.1 實作匯出 — 線上實測 12 單位、3.8 KB，含分數、無法判定數、耗時、原因與判準版本
- [x] 6.2 部分完成時匯出 — 實測 status 為 in_progress 者一併納入並標明
- [x] 6.3 實作匯入 — 實測清空 localStorage 後可完整還原作答、原因文字與耗時
- [x] 6.4 版本不符時明確告知 — 實測顯示「分數與作答維持原樣，未以目前判準重新計分」
- [x] 6.5 未匯出提示 — 作答後顯示筆數，匯出後清空；關閉頁面前提示

## 7. 接入地圖的風險圖層

- [ ] 7.1 將匯出檔置於 `app/data/risk/latest.json` 並提交（**需先實際完成評估**，不得以推算或測試資料充數）
- [x] 7.2 地圖端接入風險資料 — 檔案不存在時整層安靜停用，路段顯示「未提供」
- [x] 7.3 詳細面板呈現風險分數與其構成；未評估者維持「未提供」並說明原因。比較面板同樣接上，未評估者不以 0 參與比較
- [ ] 7.4 圖例與統計摘要納入風險分數，標明僅示範範圍已評估

## 8. 整合驗證

- [ ] 8.1 端到端：自乾淨瀏覽器開啟、評估一個單位、關閉、重開續作、匯出、清除瀏覽資料、匯入還原
- [ ] 8.2 完成全部示範單位，記錄實際耗時並據以推估擴充至更大範圍所需工時
- [ ] 8.3 於地圖確認風險圖層顯示正確，且未評估的路段仍為「未提供」
- [ ] 8.4 檢查所有對外敘述，確認在地項目未經信度檢驗、結果為單一評估者判讀這兩項限制皆有說明
