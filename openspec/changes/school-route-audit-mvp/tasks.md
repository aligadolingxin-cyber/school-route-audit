# Tasks

## 1. 專案骨架

- [ ] 1.1 建立目錄結構（`app/`、`data/units/`、`data/instrument/`、`data/records/`、`docs/instruments/`），以目錄列表確認各層存在
- [ ] 1.2 建立 `.gitignore`，排除紀錄資料夾與 token 設定檔，以 `git status` 確認兩者皆未被追蹤
- [ ] 1.3 建立極簡本機伺服器，提供靜態檔案與紀錄讀寫端點，啟動後以瀏覽器開啟首頁取得 200 回應
- [ ] 1.4 建立 Mapillary token 設定範本與讀取機制，確認缺少 token 時啟動給出明確訊息而非靜默失敗
- [ ] 1.5 撰寫 `README.md` 說明啟動方式、資料夾用途與資料授權標示，依其步驟自乾淨環境啟動成功

## 2. 判準檔

- [ ] 2.1 自 Sallis 實驗室網站下載 MAPS-Global 工具、資料字典與圖解操作手冊至 `docs/instruments/`，於同目錄記錄下載日期與來源網址
- [ ] 2.2 取得 MAPS-SRTS 論文補充材料（校門前路段區段與學校標誌項目）存入同目錄，確認含完整項目與計分說明；若補充材料未涵蓋，改以聯繫作者或退回僅用 MAPS-Global，並將結果記入 design
- [ ] 2.3 定義判準 YAML 綱要（版本號、項目識別碼、所屬區段、題型、選項、來源標記、是否計入主分、權重），撰寫綱要說明文件，以一份最小範例檔通過載入
- [ ] 2.4 轉錄 MAPS-Global 路段（30 項）與路口（23 項）區段為 YAML，標記來源 `maps-global`，以項目數與原始工具逐項核對一致
- [ ] 2.5 轉錄 MAPS-Global 土地使用（31 項）與街道設施（22 項）區段，同上核對項目數一致
- [ ] 2.6 轉錄 MAPS-Global 美觀社會區段（11 項），標記為低信度且不計入主分，載入後確認主分計算未納入此區段
- [ ] 2.7 補入 MAPS-SRTS 校門前路段區段與學校標誌項目，標記來源 `maps-srts`，確認校門前路段型別可取得完整項目集
- [ ] 2.8 讀內政部建研所「騎樓及人行道路無障礙改善研究」後擬定騎樓、巷弄、機車佔用步行空間項目，標記來源 `local`，於文件記錄各題的依據（解決 design 待決問題一）
- [ ] 2.9 加入人行道淨寬 1.5 m（受限放寬值 0.9 m）法規門檻項目，標記來源 `regulation`，確認其為客觀門檻而非主觀評分
- [ ] 2.10 實作判準檔載入與驗證，對缺漏欄位、重複項目識別碼、未知來源標記各給出指明位置的錯誤，以刻意損壞的樣本檔驗證三種錯誤皆被攔下
- [ ] 2.11 實作分離計分：產出僅含 `maps-global` 與 `maps-srts` 項目的分數，以及含 `local`、`regulation` 的完整分數，以樣本作答驗證兩者數值不同且皆可追溯至構成項目

## 3. 單位檔

- [ ] 3.1 依 D2 規則自 `daan-school-geojson` 產出大安國中與建安國小周邊候選路段（路口至路口、50–300 m、短於 50 m 併入相鄰段、長於 300 m 分割），以長度分布確認無短於 50 m 者殘留
- [ ] 3.2 依 D3 nearest-neighbor 取樣法選定納入範圍：自校門前路段起取其連接路口，再自各路口取外一層路段；以地圖目視確認涵蓋兩校校門前與外一層，且未納入建國快速道路等非步行路段
- [ ] 3.3 標定各路段的側別與各路口位置，人工指定語意識別碼，以指令檢查全檔識別碼唯一
- [ ] 3.4 定義單位檔 GeoJSON 綱要並實作載入驗證，對缺漏識別碼或型別、重複識別碼拒絕整檔載入，以刻意損壞的樣本檔驗證
- [ ] 3.5 於單位檔記錄版本號與建立方式，確認該版本可被評估紀錄引用
- [ ] 3.6 撰寫單位檔建檔說明文件，記錄本次人工建檔的判斷依據，供日後程式產出時比對

## 4. 影像候選解析

- [ ] 4.1 實作 Mapillary 查詢，依單位幾何取鄰近影像並以 `compass_angle` 篩選朝向正確側別者，以單一單位驗證取回的影像確實面向該側
- [ ] 4.2 將解析結果（影像識別碼與拍攝日期）寫入單位檔，對同一單位重跑兩次取得相同影像清單
- [ ] 4.3 產出影像覆蓋報告，列出每單位的候選影像數與最新拍攝日期，據以標出零覆蓋單位
- [ ] 4.4 人工抽查候選影像的角度與清晰度是否足以判讀，將抽查結果與不可用比例記入文件
- [ ] 4.5 對零覆蓋或影像過舊的單位建立待現地補拍清單，確認清單可被工作台讀取

## 5. 工作台

- [ ] 5.1 載入單位檔與判準檔，依單位型別呈現對應項目集合，以路口單位驗證不出現路段項目
- [ ] 5.2 嵌入 Mapillary viewer 並定位至單位位置，以路口單位驗證顯示該路口而非鄰近位置
- [ ] 5.3 實作沿路段前後移動視點，確認移動期間不離開目前單位
- [ ] 5.4 呈現目前影像的拍攝日期，切換影像時日期隨之更新
- [ ] 5.5 街景與判準表左右並排，於一般桌機解析度下兩者同時可見，作答不需捲動至影像外
- [ ] 5.6 實作單位間前後導覽與跳至指定單位，跳至已完成單位時其既有作答完整呈現
- [ ] 5.7 無可用影像的單位可標記為待現地補拍，確認其不計入已完成且不阻擋其他單位
- [ ] 5.8 呈現整體與各校完成進度，完成一個單位後數字即時更新

## 6. 紀錄保存

- [ ] 6.1 作答後即寫入該輪 JSON 檔，於作答後直接關閉瀏覽器再開啟，確認作答仍在
- [ ] 6.2 重新開啟時回到上次作業的單位並還原其作答
- [ ] 6.3 實作未開始／進行中／已完成三種狀態，確認部分項目未作答時單位維持進行中且可查出未作答項目
- [ ] 6.4 實作「無法判定」作為作答值並記錄原因，確認其計入完成度而「尚未作答」不計入
- [ ] 6.5 於每筆紀錄寫入所依據的單位檔版本與判準版本，修改判準版本後確認既有紀錄仍標明原版本
- [ ] 6.6 確認影像服務無法連線時既有紀錄仍可檢視與匯出，以中斷網路驗證

## 7. 輪次與盲化

- [ ] 7.1 開啟已完成單位時詢問檢視、修正當前輪次或開始新一輪，確認未經指定不自行判斷
- [ ] 7.2 新一輪建立獨立檔案，以檔案修改時間確認前輪檔案在新一輪期間未被寫入
- [ ] 7.3 新一輪不呈現前輪任何作答、分數或完成狀態，以已完成單位開新輪驗證所有項目為未作答
- [ ] 7.4 本輪完成後可主動檢視前輪內容，並標明此檢視發生於本輪完成之後
- [ ] 7.5 進度依當前輪次計算，對部分單位開第二輪後確認其不因第一輪已完成而顯示為完成

## 8. 耗時記錄

- [ ] 8.1 計時自單位開啟起算，於切換至其他單位或關閉應用程式時停止，以停留固定秒數驗證記錄值相符
- [ ] 8.2 切換瀏覽器分頁不暫停計時，以切走後返回驗證計時持續
- [ ] 8.3 記錄各單位被開啟的次數，多次開啟後確認次數累加
- [ ] 8.4 產出每單位耗時統計（中位數、平均、總計），以已完成單位驗證輸出

## 9. 匯出

- [ ] 9.1 決定採寬表或長表格式並將決定與理由補記於 design.md（解決 design 待決問題二）
- [ ] 9.2 實作匯出，內容含單位識別碼、所屬學校、單位型別、輪次、評估者、各項目作答值、無法判定原因、單位狀態與耗時，以匯出檔逐欄核對
- [ ] 9.3 評估未全部完成時匯出，確認已完成與進行中的單位皆納入且狀態標明
- [ ] 9.4 確認多輪紀錄於匯出後可依單位識別碼與項目識別碼逐一對照，以兩輪樣本驗證可計算一致性
- [ ] 9.5 於 README 記錄匯出欄位定義與資料來源標示要求（MAPS 授權、Mapillary CC-BY-SA、國土測繪中心、OSM ODbL）

## 10. 整合驗證

- [ ] 10.1 端到端走一遍：自乾淨環境啟動、載入單位與判準、完成一個單位、關閉、重開續作、匯出，全程無需手動修檔
- [ ] 10.2 以兩校全部單位實地試評，記錄實際耗時，回報每單位速率並據以評估擴充至 19 校所需工時
- [ ] 10.3 對單位子集於間隔後執行第二輪，匯出兩輪資料並確認可據以計算重測一致性
